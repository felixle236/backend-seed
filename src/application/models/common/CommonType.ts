export enum GenderType {
    Male = 1,
    Female
}

export enum LoginProvider {
    System = 1,
    Google,
    Facebook
}

export enum RoleCode {
    Administrator = 1,
    ProductManager = 2,
    UserCommon = 3
}
